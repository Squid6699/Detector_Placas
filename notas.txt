CREAR ENTORNO
cd backend python -m venv detectar_placas

ACTIVAR ENTORNO
cd backend detectar_placas\Scripts\activate

cd backend pip install -r requirements.txt


Para acceder a la base de datos
docker exec -it postgres_local psql -U postgres -d detector_placas

Para iniciar el contenedor
cd backend docker compose up --build

Para ver todas las tablas en postgresql
\dt 
